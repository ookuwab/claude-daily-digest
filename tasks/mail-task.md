# Mail Briefing Task

Gmail MCPでメールを確認し、要確認メールをまとめてファイルに書き出す。
Slack送信はシェルスクリプトが行うため、Claudeは送信を行わないこと。
出力は全て日本語で行うこと。

**重要: ツール制約**
- Bash、Python、シェルコマンドは一切使用しないこと（allowedToolsに含まれていないため、許可待ちでハングする）
- メールデータの処理・分類はすべてClaudeの推論能力で直接行うこと
- ファイル内容の確認には `Read` や `Grep` を使用すること

## Step 1: メール取得

`gmail_search_messages` で `after:{{FETCH_FROM_EPOCH}}` を指定してメールを検索する。maxResults: `150`。

## Step 2: メール分類

**要確認**: 個人宛メール、組織メーリングリスト、セキュリティ通知、カード利用通知、その他ユーザーが認識すべき内容
**除外**: ニュースレター、広告、プロモーション、システム自動送信、自分自身からのテスト、迷惑メール

要確認のものは、件数が多くなってしまっても必ず全てピックアップして概要をユーザーに伝えること。ユーザーの確認漏れが起きないように。

要確認メールがある場合は `gmail_read_message` で詳細を確認する。

## Step 3: ファイル出力

以下のテンプレートでメッセージを作成し、`Write` ツールで `data/mail-output.txt` に書き出すこと。

### テンプレート

要確認メールをジャンルごとにセクション分けして表示する。ジャンル分類の例は以下の通り（実際のメール内容に応じて適宜調整すること）：
- :credit_card: 決済・カード利用
- :lock: セキュリティ
- :package: 配送
- :briefcase: 仕事・業務連絡
- :bell: その他の通知

セクションごとに、受信日時の昇順で出力すること。

**決済・カード利用セクションのルール**: 個別の詳細説明ではなく、1行ずつ金額・用途・状態をコンパクトにリスト表示する。決済失敗・エラー等の対応が必要な項目は末尾に補足として記載する。

```
<@{{SLACK_USER_ID}}>
:email: *Mail Briefing - YYYY/MM/DD（曜日）*
_{{FETCH_FROM_DATE}} 以降のメールを確認_

━━━━━━━━━━━━━━━━━━━━
:credit_card: *決済・カード利用*（X件）
━━━━━━━━━━━━━━━━━━━━

• `¥X,XXX` 用途・利用先（MM/DD HH:mm）
• `$X.XX` 用途・利用先（MM/DD HH:mm）
• `$X.XX` 用途・利用先（MM/DD HH:mm）

> :warning: 補足（決済失敗への対応方法など。なければ省略）

━━━━━━━━━━━━━━━━━━━━
:lock: *セキュリティ*（X件）
━━━━━━━━━━━━━━━━━━━━

*1. 件名や概要*
> 受信日時: YYYY/MM/DD HH:mm:ss
> 送信者: 名前
> 受信者: (メールアドレス)
> 内容の要約。対応が必要な場合は何をすべきか、期限があれば明記。

━━━━━━━━━━━━━━━━━━━━
:package: *配送*（X件）
━━━━━━━━━━━━━━━━━━━━

*1. 件名や概要*
> 受信日時: YYYY/MM/DD HH:mm:ss
> 送信者: 名前
> 受信者: (メールアドレス)
> 内容の要約。

（以下、該当ジャンルがあれば同様にセクションを追加）

（要確認メールがない場合）
*確認・返信が必要なメールはありません。*

━━━━━━━━━━━━━━━━━━━━
受信メールXX件を全件確認済み。内訳は以下の通りです：
• カテゴリ1: X件
• カテゴリ2: X件

> 総括コメント（急ぎの対応、注意点など1〜2文）
```
